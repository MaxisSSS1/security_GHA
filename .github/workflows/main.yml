name: Environment
on:
  push:
  workflow_dispatch:
env:
  NODE_ENV: prodaction
  NODE_SECRET: 42
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Secrets usage
        run: |
          echo "${{ env.NODE_ENV }}"
          echo "${{ env.NODE_SECRET }}"

  
