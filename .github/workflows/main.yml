name: Environment
on:
  push:
  workflow_dispatch:
env:
  NODE_ENV: production
  NODE_SECRET: ${{ secrets.ECHO_CHECK }}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Secrets usage
        run: |
          echo "${{ env.NODE_ENV }}"
          echo "${{ env.NODE_SECRET }}"

  
